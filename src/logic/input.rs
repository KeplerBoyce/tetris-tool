use macroquad::prelude::*;
use super::Config;

pub fn handle_input(config: &Config) {
    if is_key_down(config.left) {
        
    }
    if is_key_down(config.right) {

    }
    if is_key_down(config.soft_drop) {

    }
    if is_key_down(config.hard_drop) {

    }
    if is_key_down(config.rotate_cw) {

    }
    if is_key_down(config.rotate_ccw) {

    }
    if is_key_down(config.rotate_180) {

    }
    if is_key_down(config.hold) {

    }
}
